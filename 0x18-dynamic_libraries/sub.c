#include <stdlib.h>

/**
 * sub - function subtracts one integer from the other
 * @a: first integer
 * @b: second integer
 * Return: Subtraction
 */
int sub(int a, int b)
{
        return (a - b);
}
